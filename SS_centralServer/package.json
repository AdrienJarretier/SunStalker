{
  "name": "ss-central-server",
  "version": "0.1.0",
  "description": "The server centralizing data from all arduinos connected to the network",
  "main": "SS_centralServer.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "express": "^4.16.4",
    "request": "^2.88.0",
    "request-promise-native": "^1.0.5"
  }
}
